//Store Multiple Values in one Variable using JavaScript Arrays
const myArray = ["hello", 12];

//Nest one Array within Another Array
const Array = [["sheeps", 23], ["rams", 45]];

//Access Array Data with Indexes
<p>Array indexes are written in the same bracket notation that strings use, except that instead of specifying a character, they are specifying an entry in the array. Like strings, arrays use zero-based indexing, so the first element in an array has an index of 0.</p>

const myArrayIndex = [50, 60, 70];

const myData=myArrayIndex[0];



//Modify Array Data With Indexes
const modifyArray = [18, 64, 99];
modifyArray[0]=[45];